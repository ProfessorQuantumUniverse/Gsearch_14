<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The app draws everything with Compose, so the platform theme only has to stay out
         of the way: no action bar, transparent system bars for edge-to-edge. -->
    <style name="Theme.Gsearch14" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/splash_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- Shown while the process starts; handed over to Theme.Gsearch14 by
         androidx.core.splashscreen as soon as the first frame is ready. -->
    <style name="Theme.Gsearch14.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.Gsearch14</item>
    </style>
</resources>
